import * as React from "react";
import { FunctionComponent } from "react";

import AppBar from "@material-ui/core/AppBar";

export const Header: FunctionComponent = () => {
  return <AppBar position="relative">Header</AppBar>;
};
